package dev.flavored.yap.dom

class Document : Node() {
    fun updateLayout(viewportWidth: Int, viewportHeight: Int) {
        // TODO: Build layout tree.
        // TODO: Create flex `column` formatting context.
    }

    override fun toString(): String {
        return "Document"
    }
}